programa {
  funcao inicio() {
    inteiro area, base, altura 
    escreva(" informe a base e a altura do seu triangulo: ")
    leia(base, altura)
    area = base * altura / 2
    escreva(" a area do seu triangulo eh ",area)
  }
}
